# FastAPI Todo API

A production-ready Todo API built with FastAPI.

## Quick Start

```bash
# Install dependencies
uv sync --all-extras

# Run development server
uv run uvicorn src.main:app --reload

# Run tests
uv run pytest
```

## API Documentation

Once running, visit `/docs` for interactive API documentation.
